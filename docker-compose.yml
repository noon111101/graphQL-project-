version: '3.8'

services:
  # GraphQL Backend Service
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: registry.aiunboxed.net/graphql-backend:latest
    ports:
      - "8080:8080"
    environment:
      - SERVER_PORT=8080
    networks:
      - app-network
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8080/actuator/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # REST API Backend Service
  backend-rest-api:
    build:
      context: ./backend-rest-api
      dockerfile: Dockerfile
    image: registry.aiunboxed.net/rest-api-backend:latest
    ports:
      - "8081:8081"
    environment:
      - SERVER_PORT=8081
    networks:
      - app-network
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8081/actuator/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Frontend Service
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    image: registry.aiunboxed.net/graphql-frontend:latest
    ports:
      - "3000:80"
    depends_on:
      - backend
      - backend-rest-api
    networks:
      - app-network
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:80/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

networks:
  app-network:
    driver: bridge
